import { ShowAndHiddenDirective } from './show-and-hidden.directive';

describe('ShowAndHiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAndHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
